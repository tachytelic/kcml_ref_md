Check Accelerators - Edit Caption button

This button is used to modify the caption for a control that either does not have an accelerator key or its current accelerator key is duplicated elsewhere. To add an accelerator key to the selected control insert an Ampersand (&) before a character in the controls caption. Alternatively single clicking on a control twice will also allow the accelerator key for that control to be modified.
