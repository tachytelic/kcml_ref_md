Setting the text in an Edit control

To change the text in the edit control portion of a Edit control, the

*Text\$* property is used. For example:

.EditControl1.Text\$ = "Hello World!!"

and to retrieve the text from the edit control portion of a Edit control the following could be used:

CurrentText\$ = .EditControl1.Text\$
