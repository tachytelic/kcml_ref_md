Red, Green, Blue (Color property)

Used to specify the RGB values for a color object. These must be integer values ranging from 0 to 255. For example:

.color1.red = 0 .color1.green = 255 .color1.blue = 0

would set the color to green for any controls that use the .color1 color set.
