# Registering kclient

If installed from the standard CD install or from the Internet download then kclient will automatically create all the necessary COM registry keys. If necessary it can also be manually registered with

kclient –RegServer regsvr32 klientsh.dll
