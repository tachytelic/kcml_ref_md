# <span id="thekclientactivex"></span> The Kclient ActiveX

The Kerridge client application is implemented as an inprocess DLL *klientsh.dll* that is normally started by its own container application *kclient.exe*. However the basic forms engine can be directly invoked via its COM interface.

### See also:

[ProgId](progid.htm)\
[ClassId](classid.htm)\
[Properties](properties.htm)\
[Events](events.htm)
