DDEExecute's

A DDE Execute is when a DDE client passes the server a command that it wants to execute. This scheme is often used with apps like Excel and Word to get them to execute macro commands, and is very much like the DDE Poke command. The only real difference is that no item is associated with this command - the client just connects to a particular topic and asks the server to execute a command.
