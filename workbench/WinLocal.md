## Local variables window

When stepping through a program in debug mode this window is used to show the value of the local variables. Local variables are variables that have been LOCALally DIMed in the current defsub the program is stopped in. If there are no Locally dimed variables or you don't currently reside in a defsub then no variables will be displayed.
