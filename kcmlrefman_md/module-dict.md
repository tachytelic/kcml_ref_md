# Apache Module Definitions

> Reference for Apache HTTP Server 1.3 module terminology (included in KCML CGI documentation).

## Description

This page is part of the Apache HTTP Server 1.3 documentation included with KCML's CGI/web integration documentation. It defines the terms used to describe Apache modules.

For KCML CGI configuration, see `cgi` and `httpenv`.

## See Also

- `cgi` — writing CGI scripts in KCML
- `httpenv` — CGI environment variables
- `soapserver` — KCML as a SOAP server
