# LIST ADD

> Lists line numbers of program lines that have been added or modified since the program was loaded.

## Syntax

```
LIST [title] ADD [*]
```

With `*`: lists the full text of every modified/added line.
Without `*`: lists only the line numbers.

## See Also

- `LIST LOAD` — show programs currently in memory
- `LIST` — list program source
